package com.radsoltan.strings;

// Community solution the same as mine
public class ReverseString {
    public String reverse(String inputString) {
        return new StringBuilder(inputString).reverse().toString();
    }
}
